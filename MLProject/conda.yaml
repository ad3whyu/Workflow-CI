name: car_price_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - mlflow
      - dagshub
      - pandas
      - scikit-learn
      - matplotlib
      - seaborn