name: Model Retraining & Docker Build

on:
  push:
    branches: [ main ]
    # Diperlebar agar perubahan di folder MLProject apa pun memicu retraining
    paths: 
      - 'Workflow-CI/MLProject/**'
  # Menambahkan ini agar tombol "Run workflow" muncul di tab Actions
  workflow_dispatch: 

jobs:
  retrain-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install mlflow dagshub pandas scikit-learn matplotlib seaborn docker

      - name: Run Training (MLflow Project)
        id: train_model
        env:
          DAGSHUB_USER_TOKEN: ${{ secrets.DAGSHUB_TOKEN }}
        run: |
          cd Workflow-CI/MLProject
          # Menjalankan training dan memastikan koneksi ke DagsHub
          mlflow run . -P n_estimators=120

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker Image with MLflow
        run: |
          cd Workflow-CI/MLProject
          # Menggunakan 'models:/' untuk mengambil model yang baru saja diregister 
          # atau arahkan ke uris yang tepat jika sudah didefinisikan di modelling.py
          mlflow models build-docker -m "model_retrained" -n "ad3whyu/car-price-predictor:latest" --enable-mlserver
          
      - name: Push Image to Docker Hub
        run: docker push ad3whyu/car-price-predictor:latest